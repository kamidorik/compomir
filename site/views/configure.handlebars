<div id="content">
    <h1 class="bi bi-pc-display"> Конфигуратор ПК</h1>
    <p></p>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">Название</th>
          <th scope="col">Количество</th>
          <th scope="col">Цена</th>
          <th scope="col">Действия</th>
        </tr>
      </thead>
      <tbody>
        {{#each elements}}
        <tr>
          <td>{{this.name}}</td>
          <td>1</td>
          <td>{{this.price}} р</td>
          <td><a href="/product/{{this._id}}" class="bi bi-card-image"></a></td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    <h1>Общая цена: {{price}} р</h1>
    <form action="/delete_configurator" method="post"><input type="submit" value="Очистить список" class="btn btn-outline-dark"></form>
</div>